# TODO

## Product Specification

- Define the canonical upstream or source product for `miku-text-bundle`.
- Define the primary input and output artifacts.
- Decide the initial CLI command contract.
- Add product core APIs after the input/output contract is settled.

## Java Application

- Add upstream class mapping under `docs/` when conversion source files are known.
- Add upstream test mapping under `docs/` when upstream test intent is known.
- Add fixtures for deterministic product behavior.
- Expand CLI tests to cover product commands, generated files, diagnostics, and exit codes.

## Packaging

- Decide whether a distribution zip is needed in addition to the executable jar.
