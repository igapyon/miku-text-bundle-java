# Release Notes: v1.1.1

`v1.1.1` follows upstream `miku-text-bundle` `v1.1.1` and keeps generated Markdown behavior unchanged from `v1.1.0`.

## Changes

- Updated the project and CLI version to `1.1.1`.
- Aligned CLI help generated-artifact wording with upstream:
  - The first part includes the prompt instructions.
  - The final part includes the terminal index.
- Removed the old Web UI prompt-pasting recommendation from CLI help.

## Compatibility Notes

Generated file names and Markdown bundle semantics are unchanged from `v1.1.0`.
